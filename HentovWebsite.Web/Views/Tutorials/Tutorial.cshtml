@model HentovWebsite.Models.View.Tutorials.TutorialViewModel

@{
    ViewBag.Title = @Model.Title;
}

<div class="container">
    <div class="col-md-12">
        <h1>@Model.Title</h1>
        <div>
            <iframe width="720" height="405" src="@String.Format("https://www.youtube.com/embed/{0}", @Model.VideoId)"></iframe>
        </div>
        <p> @Model.Description</p>
        <hr>
    </div>
</div>


<p>
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-submit" })
</p>
